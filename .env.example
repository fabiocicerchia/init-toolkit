# init-toolkit's own scripts take CLI args, not environment variables.
# `dockerize` templates *your* config files from *your* container's env — see
# docs/ for that usage. This file exists so the layout matches the other repos.

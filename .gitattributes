# Normalize line endings
* text=auto eol=lf

# Shell helpers must stay LF even on Windows checkouts
bin/* text eol=lf

# Binary files (no diff/merge)
*.png binary
*.jpg binary
*.gif binary
*.pdf binary
*.zip binary
